name: NavoTap App Icon Audit

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/navotap-icon-audit.yml'

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical diagnostic complete: NavoTap build 5 compiled AppIcon was fully black. Replacement work moved to NavoTap build 6.'
