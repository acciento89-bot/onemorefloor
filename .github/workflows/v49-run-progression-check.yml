name: Run Progression Presentation v1.36 Check

on:
  push:
    branches:
      - main
      - agent/**
  pull_request:

jobs:
  validate-v49:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download Godot 4.7.1
        run: |
          curl -L --fail --retry 3 -o godot.zip https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -q godot.zip
          mv Godot_v4.7.1-stable_linux.x86_64 godot
          chmod +x godot

      - name: Parse v1.36 runtime and reward art
        run: |
          set -o pipefail
          ./godot --headless --editor --path . --quit 2>&1 | tee parse.log
          ./godot --headless --path . --script res://scripts/main_v49.gd --check-only
          ./godot --headless --path . --script res://scripts/v49_run_progression_smoke_test.gd --check-only
          if grep -E "SCRIPT ERROR|Parse Error|Compile Error|Failed to load script|Error.*SVG" parse.log; then
            exit 1
          fi

      - name: Run v1.36 run progression smoke test
        run: |
          set -o pipefail
          ./godot --headless --path . --script res://scripts/v49_run_progression_smoke_test.gd --quit-after 120 | tee v49.log
          grep -q "ONE MORE FLOOR v1.36 run progression presentation smoke test passed" v49.log
